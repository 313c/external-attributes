require "spec_helper"

describe ExternalAttributes do
  it "has a version number" do
    expect(ExternalAttributes::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
